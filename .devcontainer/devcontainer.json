{
  "name": "open-modelica",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "pipx install uv && (uv venv --allow-existing 2>/dev/null || uv venv) && uv pip install -e '.[dev]'"
}